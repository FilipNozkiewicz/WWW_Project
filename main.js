$(function(){  // load when document.ready // when documnet is reeady
		init(); // init is casual function and it wont be called automaticaly
		console.log("Main log called");
});

function init(){
	console.log("init() called");
}